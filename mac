#!/usr/bin/env bash
if [[ -z $STOW_FOLDERS ]]; then
    STOW_FOLDERS="alacritty,atuin,aws,bash,bin,clangd,k9s,lazydocker,lazygit,markdownlint,mcphub,nvim,rclone,ssh,starship,textlint,tmux,yazi,zsh"
fi

if [[ -z $DOTFILES ]]; then
    DOTFILES=$HOME/Privatespace/.dotfiles
fi

STOW_FOLDERS=$STOW_FOLDERS DOTFILES=$DOTFILES $DOTFILES/install
